# Database URL from Render PostgreSQL
DATABASE_URL=postgresql://username:password@host:port/database

# Example for Render:
# DATABASE_URL=postgresql://kltn_user:xxxxx@dpg-xxxxx.oregon-postgres.render.com/kltn_db

# Thay đổi smoothSteps:
smoothSteps=2  # Tăng nhanh hơn (2 ngày transition)
smoothSteps=5  # Tăng chậm hơn (5 ngày transition)
smoothSteps=Math.min(3, mlPredictions.length) # Hiện tại (3 ngày)
